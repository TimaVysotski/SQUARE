interface SuareSizesProps {
  baseMargin: number;
  basePadding: number;
  baseBorderRadius: number;
}

const sizes: SuareSizesProps = {
  baseMargin: 8,
  basePadding: 8,
  baseBorderRadius: 15,
};

export default sizes;
